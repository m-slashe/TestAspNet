using System.Web;
using Teste.Models;

namespace Teste
{
    public class Global : HttpApplication
    {
        protected void Application_Start()
        {
            /*PizzaContext context = new PizzaContext();
            Pizza pizza = new Pizza("mussarela", 5.5f);
            context.Pizzas.Add(pizza);
            context.SaveChanges();*/
        }
    }
}
